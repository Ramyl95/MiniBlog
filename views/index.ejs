<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Mon Blog</title>
</head>
<body>
  <div class="home-container">
  <h1 class="mb-4">Blog d'informatique</h1>

<div class="mb-4">
  <a href="/new" class="btn btn-primary">➕ Nouvel article</a>
</div>

<ul class="list-group">
  <% articles.forEach(article => { %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <a href="/article/<%= article.id %>" class="fw-bold text-decoration-none"><%= article.title %></a>
      <a href="/delete/<%= article.id %>" class="btn btn-danger btn-sm" onclick="return confirm('Supprimer cet article ?')">🗑 Supprimer</a>
    </li>
  <% }) %>
</ul>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>