<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <title><%= article.title %></title>
</head>
<body>
  <h1><%= article.title %></h1>
<p><%= article.content %></p>

<hr>
<h3>Commentaires</h3>
<ul class="list-group mb-3">
  <ul class="list-group mb-3">
  <% comments.forEach(comment => { %>
    <li class="list-group-item">
      <strong><%= comment.author %> :</strong> <%= comment.comm %>
    </li>
  <% }) %>
</ul>
</ul>

<form method="post" action="/article/<%= article.id %>/comment" class="mb-4">
  <div class="mb-2">
    <input type="text" name="author" class="form-control" placeholder="Votre nom" required>
  </div>
  <div class="mb-2">
    <textarea name="text" class="form-control" placeholder="Votre commentaire" required></textarea>
  </div>
  <button type="submit" class="btn btn-success">Ajouter un commentaire</button>
</form>

<a href="/" class="btn btn-secondary">Retour</a>
</form>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
